# Streamlit
Learning methods to build an application and deploy it on the opensource cloud infrastructure.
# Git Commnands applied
1. git add .
2. git commit -m initial commit
3. git push origin main
# Commands to run this app from commandline
streamlit run app.py
